
<div class="container-md mt-2 bg-light p-5">
  {{> backToDashboard}}

  <div class="mx-2 border-bottom">
    <h4 class="lh-base me-5">{{userFN}}, create a new event in step one. Add a seating chart to that event in step two. Then head to <a class="text-dark" href='/viewseating/{{adminId}}'>View/Edit Seating</a> to see your seating chart. </h4>
  </div>

  <div class="row d-flex justify-content-evenly">

    {{> chaircard}}

    <div class="col-md-6">
      <form class="mt-3 border rounded p-2 bg-secondary">
        <h5 class="fw-bold">Step 1- Name your event</h5>
        <div class="mb-3">
          <label for="name-signup">Event Name</label>
          <input type="text" class="form-control" id="eventName" placeholder="Birthday">
        </div>
          <button type="submit" class="btn btn-danger text-light register">Name Event</button>
      </form>
      {{> createseating}}

</div>
  <script src='/js/createEvent.js'></script>